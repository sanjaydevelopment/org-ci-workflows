name: Python Project Checklist

on:
  workflow_call:
    inputs:
      project_folder:
        required: true
        type: string
        description: "Main project folder (e.g., AILogAnalyzer)"

jobs:
  checklist:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install tools
        run: |
          python -m pip install --upgrade pip
          pip install flake8 bandit

      - name: Check required files
        id: files
        run: |
          msg=""
          if [ ! -f requirements.txt ]; then msg="❌ requirements.txt missing"; fi
          if [ ! -f README.md ]; then msg="$msg\n❌ README.md missing"; fi
          if [ -n "$msg" ]; then echo -e "$msg"; exit 1; fi
          echo "✅ Required files found."

      - name: Check project structure
        id: structure
        run: |
          msg=""
          if [ ! -d "${{ inputs.project_folder }}" ]; then msg="❌ Folder ${{ inputs.project_folder }}/ missing"; fi
          if [ ! -d tests ]; then msg="$msg\n❌ tests/ folder missing"; fi
          if [ -n "$msg" ]; then echo -e "$msg"; exit 1; fi
          echo "✅ Project structure looks good."

      - name: Check for hardcoded values
        id: hardcode
        run: |
          if grep -i -E "password|secret|apikey|token|dbhost" -r ${{ inputs.project_folder }}/; then
            echo "❌ Hardcoded sensitive value found. Please use environment variables."
            exit 1
          fi
          echo "✅ No hardcoded sensitive values found."

      - name: Lint with flake8
        id: lint
        run: |
          flake8 ${{ inputs.project_folder }} --max-line-length=120 || (
            echo "❌ Linting failed. Please fix style issues." && exit 1
          )
          echo "✅ Lint passed."

      - name: Security check with Bandit
        id: bandit
        run: |
          bandit -r ${{ inputs.project_folder }} || (
            echo "❌ Security issues found!" && exit 1
          )
          echo "✅ Security scan passed."

      # ✅ Final summary
      - name: Summary Report
        if: always()
        run: |
          echo "📋 Project Checklist Summary"
          echo "---------------------------"
          echo "Files Check:     ${{ steps.files.outcome }}"
          echo "Structure Check: ${{ steps.structure.outcome }}"
          echo "Hardcoded Scan:  ${{ steps.hardcode.outcome }}"
          echo "Lint Check:      ${{ steps.lint.outcome }}"
          echo "Security Check:  ${{ steps.bandit.outcome }}"
